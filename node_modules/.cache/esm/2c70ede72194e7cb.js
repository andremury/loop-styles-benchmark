let Worker;_219‍.w("worker_threads",[["Worker",["Worker"],function(v){Worker=v}]]);

const workers = [
  new Worker("./while.js"),
  new Worker("./interval.js"),
  new Worker("./timeout.js"),
];

workers.forEach((worker) => {
  worker.on("message", (d) => {
    _219‍.g.console.log(d);
  });
});
